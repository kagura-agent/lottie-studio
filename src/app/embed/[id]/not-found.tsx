export default function EmbedNotFound() {
  return (
    <div
      style={{
        position: "fixed",
        inset: 0,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "system-ui, sans-serif",
        color: "#a1a1aa",
        fontSize: "14px",
      }}
    >
      Animation not found
    </div>
  );
}
